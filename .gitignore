*.pyc
cachefile.dbm
bazarr.pid

/venv
/data
/bin

# IDE
.idea
.vscode

# LSP
pyrightconfig.json

# Middleware
VERSION

# CI
/__builds__
/package.json
/package-lock.json

# Allow
!*.dll

.claude/
.superpowers/
docs/superpowers/
.coverage
.playwright-mcp
.codex
.agents
.pytest_cache
.ruff_cache

# Vitest run from the repo root (instead of frontend/) writes its cache
# here. The frontend tree has its own node_modules ignore already.
/node_modules

# Git worktrees
.worktrees/

# Release hero renders and test-server captures. Local artefacts: the hero
# pipeline regenerates them, and the captures can carry LAN detail.
screenshot/release/

# Third-party tooling clones and local agent config. These are working-copy
# conveniences that must never become part of the published source.
claude-remotion-skill/
.github/hooks/
